package com.solvd.laba.bank.worker.operation;

import com.solvd.laba.bank.worker.Worker;

public class Cleaner extends Worker {
    public Cleaner(int ID, String fullName) {
        super(ID, fullName);
    }
}
